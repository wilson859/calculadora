import pandas as pd
import random

# Función para generar fechas y horas aleatorias
def generar_fecha_hora():
  # ... (Igual que en el ejemplo anterior)

# Generar datos aleatorios para 1000 filas
data = {'fecha_hora': [generar_fecha_hora() for _ in range(1000)],
        'operacion': ['suma', 'resta', 'multiplicacion', 'division'] * 250,
        'operando1': [random.randint(1, 100) for _ in range(1000)],
        'operando2': [random.randint(1, 100) for _ in range(1000)],
        'resultado': [random.randint(1, 1000) for _ in range(1000)],
        'usuario': ['usuario1', 'usuario2', 'usuario3'] * 333}

# Crear un DataFrame de pandas
df = pd.DataFrame(data)

# Guardar como CSV
df.to_csv('historial_calculos.csv', index=False)

print("Archivo CSV generado correctamente.")
